import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Dutch Box 3 Tax Calculator (2025) — Notional Return Estimator | Boursee',
  description: 'Estimate your Dutch Box 3 wealth tax under the notional return system for the 2025 tax year: savings, investments and debts, with the fictitious-return rates applied.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
